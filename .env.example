# Maximum number of conversations/facts/todos printed per category when
# running with --include-content. Kept small by default so an opted-in
# content view stays a deliberate, glanceable snapshot.
LOOP_MAX_ITEMS=5
